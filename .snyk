# Snyk (https://snyk.io) policy file
#
# Ignored vulnerabilities and license issues
#
# False Positives:
# The `innerHTML` injection in js/app.js is fetching trusted
# local HTML static assets (sections/*.html), not user input.
# Therefore, DOM-based Cross-site Scripting (DOMXSS) alerts are false positives.

version: v1.25.0
ignore:
  snyk:
    - DOM-based Cross-site Scripting (XSS):
        - js/app.js:
            - reason: "Data is fetched from trusted local static templates, not external user input. Safe context."
              expires: 2040-01-01T00:00:00.000Z
